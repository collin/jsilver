describe('Sample', function() {
  it("does equality", function() {
    expect(0).to(equal, 1);
  });
  
  it("does equality", function() {
    expect([1,2,4]).to(equal, [4,5,6]);
  });
  
  it("does equality", function() {
    expect(0).to(equal, 4);
  });
  
  it("does equality", function() {
    expect([1,2,4]).to(equal, [4,5,6]);
  });
});

